export class User {
    name: string
    lastName: string
    rol: string

    constructor(name: string, lastName: string, rol: string){
        this.name = name
        this.lastName = lastName
        this.rol = rol
    }
}